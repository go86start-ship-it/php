git hub 
<現在値を確認>
pwd
<ローカルのパソコンに保存>---------------
<カレントディレクトリに移動>
<現在地確認＞
pwd 
<カレントディレクト移動>
cd ../../
<リポジトリに移動>
cd "パス名"
<リポジトリロード>
git pull --rebase origin main

<更新されているか確認>
git status

<処理が終わらない場合>
!+Enterかctrl＋C

<git hubに保存>------------------
<ブランチに保存>
git add .
<リポジトリに保存>
git commit -m "コメント"
 
<リポジトリに保存>
git push origin main(ブランチ名)

<ローカルパソコンに保存>----------------------
<カレントディレクトリに移動>
<現在地確認＞
pwd
<カレントディレクト移動>
cd ../../
<リポジトリに移動>
cd "C:\jisyu\intecs-Yumi-intecs-TeamB-Javaexercises"
<リポジトリロード>
git pull --rebase origin main

<更新されているか確認>
git status
/コンフリクト
コンフリクトが発生する理由は大きくいうと、merge、cherry-pick、rebase、pull 時に「同じ場所を異なる方法で変更した」こと。
発生例１同じ行
発生条件: 複数のブランチで同じファイルの同じ行を異なる内容で変更した場合。

発生例２同じファイルで依存関係などがある
発生条件: 同じファイルが異なるブランチで変更された場合でも、変更箇所が異なればGitは自動でマージできる。
ただし、変更の内容に依存関係があったり、同じ部分で変更が重なる場合に競合が発生します。

発生例３　ファイル削除と変更の競合
発生条件: 片方のブランチでファイルを削除したのに、他のブランチでそのファイルに変更を加えた場合。

発生例４　ファイル追加と削除の競合
発生条件: 一方のブランチでファイルを追加し、もう一方でそのファイルを削除した場合。
例: feature-branchで abc.py を新しく追加し、mainで abc.py を削除した場合。


発生例５　同じファイルを移動またはファイル変更
発生条件: 同じファイルが異なるブランチで移動または名前を変更され、両方の変更がマージされるとコンフリクトが発生します。
例: feature-branchで abc.py をdef.py にリネームし、mainで abc.py を別のフォルダに移動した場合。

発生例６　ブランチ間の履歴の違い
発生条件: 複数のブランチで独立して変更が進んだ場合、特に異なる開発者が同時に別々の変更を行うと履歴にギャップが生まれることがあります。
これが後で統合しようとするときにコンフリクトを引き起こすことがあります。

コンフリクト部分を手動で解消する
コンフリクトが発生したファイルを開くと、次のような形式でマーカーが表示されます。

abc.py
<<<<<<< HEAD
# mainブランチ
def calc(x, y): 
    result = x + y
    print("Main branch: Adding x and y:", result)
    return result
=======
# featureブランチ
def calc(x, y):
    result = x + y
    print("Feature branch: Adding x and y:", result)
    result_squared = result ** 2
    print("Squared result:", result_squared)
    return result_squared
>>>>>>> feature

<<<<<<< HEAD から ======= までは現在のmainブランチ。
======= から >>>>>>> feature までは、マージ元のfeatureブランチです。
ここで、どの変更を採用するか、あるいは両方を組み合わせるかを手動で判断して修正します。

修正後、マーカー部分（<<<<<<<, =======, >>>>>>>）を削除します。

//ワークツリー(ローカルパソコン）➡ add/ ステージングエリア➡ commit /リポジトリ（ブラウザ）
git␣add␣.                                     git␣commit␣-m"コミットメッセージ"







/ワークツリー(ローカルパソコン）➡ add/ ステージングエリア➡ commit /リポジトリ（ブラウザ）
git␣add␣.                                     git␣commit␣-m"コミットメッセージ"